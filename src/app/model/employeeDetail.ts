export interface EmployeeDetail {
    id: number,
    currentAdd: string,
    permanentAdd: string,
    familyMembers: string,
    createdAt: Date,
    updatedAt: Date,
    createdBy: number,
    updatedBy: number,
    isActive: Boolean,
    deleted: Boolean
}
